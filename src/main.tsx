import "@/index.css";

import { createRoot } from "react-dom/client";

import App from "@/App";

createRoot(document.getElementById("root")!).render(<App />);

// TODO:
// * Code split into smaller chunks
// * Compress every audio file, picture etc.
// * Black hole zoom out the end ending
